/**
 * Temporary company-side rollout gate for Google Calendar and meeting scheduling.
 * Keep the implementation in place behind one switch so it can be paused again
 * without removing the company UI or company-side LLM paths.
 */
export const COMPANY_MEETING_SCHEDULING_ENABLED = true;
