/**
 * Temporary company-side rollout gate for Google Calendar and meeting scheduling.
 * Keep the implementation in place, but do not expose it until the flow is ready
 * to give to companies. Set this to true to restore the existing surfaces.
 */
export const COMPANY_MEETING_SCHEDULING_ENABLED = false;
