# Server-only Composio Gmail integration
COMPOSIO_API_KEY=""
COMPOSIO_GMAIL_AUTH_CONFIG_ID="ac_..."

# Optional return-redirect white-labeling (configure this exact URL in both
# Google Cloud and the Composio Gmail Auth Config):
# https://<your-domain>/api/talent/integrations/gmail/oauth/callback
